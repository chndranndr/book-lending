services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:sqlite:/data/library.db
    volumes:
      - library-data:/data

volumes:
  library-data:
